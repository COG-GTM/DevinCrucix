"""CRUCIX border-region ingestion subsystem.

Collects, cleans, deduplicates and entity-extracts news articles from a
data-driven source registry, and loads structured baseline datasets so the
dashboard can flag anomalies relative to historical norms.
"""

__version__ = "0.1.0"
